__pycache__/
*.pyc
.venv/
venv/
.env
.dev.vars
data/
*.db
*.mp3

# Fotos de prueba que se dejan caer en la carpeta al probar "describe"
images.jpg

# Modelos de Piper: 63 MB cada uno, se bajan solos al arrancar
voices/
*.wav

# Clips generados con generar_clips.py. No van al repositorio: el dispositivo
# se los baja del backend con /speak, y así siempre son la voz que hay puesta.
assets/

# Sesiones del panel /admin (NiceGUI las guarda en el directorio de trabajo)
.nicegui/
data/captures/
captures/
