fastapi==0.141.1
uvicorn[standard]==0.52.0
httpx==0.28.1
# TTS en local, el único. Arrastra onnxruntime, que son ~58 MB en la imagen: es
# el precio de bajar el TTS de 1.320 ms a 205 ms y no depender de Microsoft.
piper-tts==1.6.0
# Para reducir la foto antes de mandarla a Groq, que cobra por píxeles.
pillow==12.3.0
pydantic==2.13.4
# Panel de administración de la base de datos (/admin). Sirve Vue y Quasar
# desde el propio paquete: no depende de ningún CDN. Solo se importa si
# ADMIN_PASSWORD está definida.
nicegui==3.15.0
